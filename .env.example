# Neon PostgreSQL — copy connection string from Neon dashboard
DATABASE_URL=

# SMTP credentials
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=scanner@vindemiapartners.com

# Comma-separated recipient list (no extra spaces)
NOTIFY_TO=wouter.vanderveken@vindemiapartners.com,kasper.derycke@vindemiapartners.com
